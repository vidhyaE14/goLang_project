apiVersion: backstage.io/v1alpha1
kind: Component
metadata:
  name: my-service-vault
  annotations:
    vault.io/secrets-path: test/backstage, test/backstage2
    vault.io/secrets-engine: secrets
spec:
  type: service
  owner: my-vault-team
  lifecycle: production
